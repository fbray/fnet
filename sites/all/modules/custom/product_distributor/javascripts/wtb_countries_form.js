jQuery.noConflict();
(function ($) {
    $(function() {
        /*
        $('input[type=checkbox]').click(function () {
            if ($(this).attr("checked")) {
                var currId = $(this).attr('id');
                var key = /skip-region/;
                var tmpId = null;
                if (currId.search(key) != -1) {
                    tmpId = currId.replace('skip-region', 'show-always');
                } else {
                    tmpId = currId.replace('show-always', 'skip-region');
                }
                $('#' + tmpId).removeAttr('checked');
                $('#'.currId).attr('checked', 'checked');
            }
        }); */
    });
})(jQuery);